{% extends 'base.html.twig' %}

{% block title %}Edit Map
{% endblock %}

{% block body %}
	<main id="main-content" class="map-bg">
		<div class="map-child">

			<h1>Editer la carte</h1>

			{{ include('map/_form.html.twig', {'button_label': 'Update'}) }}
			{{ include('map/_delete_form.html.twig') }}
			<button>
				<a href="{{ path('app_map_index') }}">Retour à la liste</a>
			</button>
		</div>
	</main>

{% endblock %}
