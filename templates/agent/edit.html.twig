{% extends 'base.html.twig' %}

{% block title %}Editer Agent{% endblock %}

{% block body %}
	<main id="main-content" class="agent-bg">
		<div class="map-child">

    <h1>Editer Agent</h1>

    {{ include('agent/_form.html.twig', {'button_label': 'Update'}) }}
    {{ include('agent/_delete_form.html.twig') }}
<button>
    <a href="{{ path('app_agent_index') }}">Retour à la liste</a>
</button>
        </div>
    </main>
{% endblock %}
